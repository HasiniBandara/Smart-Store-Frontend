# Setup
* npm create vite@latest smart-store-frontend -- --template react-ts
* npm install react-router-dom
* npm install -D tailwindcss postcss autoprefixer
* npx tailwindcss init -p
// if not works use CLI & run tailwind 


